fn fib(n) {
    if (n < 2) { return n; }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    print("Argon: Starting Fib(35)...");
    let start = timestampMs();
    let res = fib(35);
    let end = timestampMs();
    print("Argon: Result = " + res);
    print("Argon: Time = " + (end - start) + "ms");
}
