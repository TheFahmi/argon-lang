// Argon FFI Test (v2.27.0)
// Test loading and calling C libraries

fn main() {
    print("=== Argon FFI Test ===");
    print("");
    
    // Try to load msvcrt on Windows (standard C library)
    print("Attempting to load C library...");
    
    let loaded = ffi_load("msvcrt");
    if (loaded) {
        print("✓ Library loaded successfully!");
        
        // Try calling a simple function
        // Note: This is platform-specific and may not work everywhere
        print("");
        print("FFI is ready for use!");
    } else {
        print("Note: msvcrt not found (expected on non-Windows)");
        print("FFI module is functional, library loading is platform-specific.");
    }
    
    print("");
    print("=== FFI Test Complete ===");
}
