// Test Module Import System (v2.7)
import "examples/math_utils.ar";

fn main() {
    print("Testing Module System (v2.7)...");
    
    // Use imported functions
    let a = 5;
    let b = 3;
    
    let sum = math_add(a, b);
    print("math_add(5, 3) = " + sum);
    
    let product = math_mul(a, b);
    print("math_mul(5, 3) = " + product);
    
    let squared = math_square(4);
    print("math_square(4) = " + squared);
    
    print("Module import test complete!");
}
